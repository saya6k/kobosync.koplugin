local _ = require("kobosync_gettext")
return {
    name = "kobosync",
    fullname = _("Kobo Sync"),
    version = "0.4.0",
    description = _([[Synchronizes library and reading progress with a self-hosted Kobo Sync server ]]
        .. [[such as calibre-web.]]),
}
